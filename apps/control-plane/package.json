{
  "name": "@lghs/control-plane",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@lghs/adapter-discord": "workspace:*",
    "@lghs/adapter-dynamodb": "workspace:*",
    "@lghs/adapter-ec2": "workspace:*",
    "@lghs/adapter-minecraft": "workspace:*",
    "@lghs/adapter-s3": "workspace:*",
    "@lghs/core": "workspace:*"
  }
}
